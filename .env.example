# No API keys required for default TV setup (TVMaze + public torrent APIs).
# Movies: paste a free TMDB API key in Settings (https://www.themoviedb.org/settings/api).
# Optional: only if you enable Jackett in Settings.
